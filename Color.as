/*
 * Custom sets of Colors using in this plugin.
 */
namespace Color {
    const vec4 RED         = vec4(1, 0, 0, 1);
    const vec4 DELTA_RED   = vec4(1, 0.3, 0.3, 1);
    const vec4 DELTA_BLUE  = vec4(0.3, 0.6, 1, 1);
    const vec4 LIGHT_GRAY  = vec4(0.8, 0.8, 0.8, 1);
}